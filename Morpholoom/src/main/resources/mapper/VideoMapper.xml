<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.Morpholoom.mapper.VideoMapper">

    <resultMap id="VideoResult" type="com.project.Morpholoom.domain.VideoAsset">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="fileUrl" column="file_url"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertVideo" parameterType="com.project.Morpholoom.domain.VideoAsset" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO videos (user_id, file_url, created_at)
        VALUES (#{userId}, #{fileUrl}, #{createdAt})
    </insert>

    <delete id="deleteVideo" parameterType="long">
        DELETE FROM videos WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="VideoResult">
        SELECT * FROM videos WHERE id = #{id}
    </select>

</mapper>

