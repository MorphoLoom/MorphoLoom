<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.Morpholoom.mapper.CreationLikeMapper">

    <select id="exists" resultType="boolean">
        SELECT COUNT(1) > 0 FROM creation_likes WHERE user_id = #{userId} AND creation_id = #{creationId}
    </select>

    <insert id="insert">
        INSERT INTO creation_likes (user_id, creation_id, created_at)
        VALUES (#{userId}, #{creationId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM creation_likes WHERE user_id = #{userId} AND creation_id = #{creationId}
    </delete>

    <select id="countLikes" resultType="int">
        SELECT COUNT(*) FROM creation_likes WHERE creation_id = #{creationId}
    </select>

</mapper>

