<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.Morpholoom.mapper.ImageMapper">

    <resultMap id="ImageResult" type="com.project.Morpholoom.domain.ImageAsset">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="fileUrl" column="file_url"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertImage" parameterType="com.project.Morpholoom.domain.ImageAsset" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO images (user_id, file_url, created_at)
        VALUES (#{userId}, #{fileUrl}, #{createdAt})
    </insert>

    <delete id="deleteImage" parameterType="long">
        DELETE FROM images WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="ImageResult">
        SELECT * FROM images WHERE id = #{id}
    </select>

</mapper>

